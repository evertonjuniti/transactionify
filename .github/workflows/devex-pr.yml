# DevEx PR Pipeline — transactionify
#
# Generated by `devex init` on 2026-05-10.
# Do NOT edit the workflow logic here. Instead, modify devex.yaml or
# upgrade the framework ref with: devex upgrade --workflow-version <tag>
#
# Language adapter activated: python (via runtime.appLanguage in devex.yaml)

name: DevEx PR Pipeline

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, edited]

permissions:
  contents: read
  pull-requests: write
  checks: write
  id-token: write

jobs:
  devex-pr:
    uses: evertonjuniti/loanprochallenge/.github/workflows/pr.yml@v0.3.2
    with:
      config-path: devex.yaml
    secrets: inherit
